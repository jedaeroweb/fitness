<section id="show-group" class="container">
  <div class="row">
    <%= render :partial => 'layouts/notice', :locals => { :errors => @group.errors, :notice => notice, :alert => alert } %>
  </div>
  <div class="row">
    <div class="col-12">
      <div class="card">
        <h3 class="card-header"><%=@group.title %></h3>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-12 col-xl-6">
      <%= link_to t(:link_list), admin_groups_path, class: 'btn btn-secondary' %>
    </div>
    <div class="col-12 col-xl-6 text-right">
      <%= link_to t(:edit), edit_admin_group_path(@group), class: 'btn btn-secondary' %>
      <%= link_to t(:delete), admin_group_path(@group), class: 'btn btn-danger' %>
    </div>
  </div>
</section>
